# ─────────────────────────────────────────────────────────────────────────────
# Skill Assessment App — environment variables
#
# Copy this file to `.env.local` and fill in real values. `.env.local` is
# gitignored. Never commit real secrets.
# ─────────────────────────────────────────────────────────────────────────────

# Postgres (Supabase)
# Project Settings → Database → Connection string → URI.
# For Drizzle migrations + app runtime, use the "Session" pooler URI on
# port 5432, or the direct connection. The "Transaction" pooler (port 6543)
# also works because we set `prepare: false` in src/lib/server/db/client.ts.
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"

# Auth.js (NextAuth)
# Generate a 32-byte random secret:  openssl rand -base64 32
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET=""

# Google OAuth
# Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client ID.
# Authorized redirect URI: http://localhost:3000/api/auth/callback/google
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# OpenAI
# Required starting in Phase 5 (LLM generation/scoring). Safe to leave blank
# through Phase 4.
OPENAI_API_KEY=""

# Optional. Override the OpenAI model id. Defaults to the current gpt-4o
# snapshot in src/lib/server/llm/client.ts. Useful for cost knobs in dev or
# staging. Must be a model that supports structured outputs (json_schema with
# strict: true) — e.g. gpt-4o, gpt-4o-mini, or any 2024-08-06+ snapshot.
OPENAI_MODEL=""
